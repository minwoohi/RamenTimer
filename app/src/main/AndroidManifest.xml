<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ktds.ramentimer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cs"
        android:label="라면 타이머"
        android:roundIcon="@mipmap/cs"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MyRamenTimerCounterService"
            android:enabled="true"
            android:exported="true"/>

        <activity android:name=".SinRamenActivity" />
        <activity android:name=".BibimMyeonActivity" />
        <activity android:name=".GarnJjarmBbongActivity" />

    </application>

</manifest>